

  #16

  # year field:
    + Here we are explicitly telling year field to be treated as date

  # -H "Content-Type ..."
    + curl also requires content-type which is not shown in the snapshot
      (There is a way to set this universal so that we do not have to type all the time)